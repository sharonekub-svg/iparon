# שינון

אפליקציית לימודים בעברית לתלמידי תיכון. התלמיד מעלה PDF או מצלם דף סיכום,
והמערכת מחזירה שלושה דברים: סיכום מסודר, כרטיסיות שאלה־תשובה, וקוויז אמריקאי.
הכול בעברית, והכול מבוסס אך ורק על החומר שהועלה.

## מה קיים כרגע

שלבים 3 ו-4: השלד, מסך הבית, מסך ההעלאה, והשרת שמדבר עם המודל.

- Expo (React Native) + TypeScript, ניווט קבצים עם expo-router
- RTL מלא, נאכף לפני הרינדור הראשון
- מערכת העיצוב מ-`design/`, מומשת בטוקנים: Assistant + JetBrains Mono,
  פלטת הנייר והדיו, רקע גרעיני
- מסך בית: רשימת החומרים, מצב ריק, משיכה לרענון
- מסך העלאה: צילום דף או בחירת קובץ, המרה ל-base64 ושליחה לשרת
- Edge Function `analyze`: שולחת את הקובץ למודל ראייה ומחזירה את ה-JSON
- Edge Function `materials`: רשימת החומרים וחומר בודד
- תקרה חודשית על ההוצאה, נבדקת לפני כל קריאה למודל
- מסך תוצאה עם שלושה טאבים: סיכום, כרטיסיות, קוויז
- כרטיסיות: החלקה ימינה־שמאלה, הקשה להפיכה
- קוויז: שאלה אחת במסך, משוב מיד, ניקוד בסוף
- אתר נחיתה סטטי ב-`site/`, מוכן לוורסל

**מה עוד לא קיים:** פריסה בפועל — אין פרויקט Supabase, ולכן ההעלאה עוד לא
עבדה מקצה לקצה. `EXPO_PUBLIC_SEED_DEMO=1` מריץ את כל המסכים על חומר לדוגמה
בלי שרת.

## הרצה

```bash
npm install
npm start          # ואז i / a לפתיחה במדמה
npm run typecheck
```

בשביל שההעלאה תעבוד צריך `.env` עם הפרטים של פרויקט ה-Supabase — ראה
`.env.example`. בלי זה האפליקציה עולה ועובדת, אבל אין לה לאן לשלוח.

להצגת חומרי הדגמה במסך הבית בזמן פיתוח, בלי שרת:

```bash
EXPO_PUBLIC_SEED_DEMO=1 npm start
```

## פריסה

| מה | איפה | פקודות |
|---|---|---|
| סכימה + פונקציות | Supabase | `supabase/README.md` |
| אתר הנחיתה | Vercel | `site/README.md` |
| האפליקציה | EAS / TestFlight | שלב 7 |

## כללי ברזל

1. **מפתח ה־API של המודל נמצא רק ב־Secrets של ה־Edge Function**, אף פעם לא בקוד
   האפליקציה. `assertNoModelKeyInApp()` ב־`src/config/env.ts` נופל בזמן פיתוח אם
   מפתח כזה מסתנן ל־`EXPO_PUBLIC_*`.
2. האפליקציה שולחת את התמונה/PDF לפונקציה. הפונקציה היחידה שמדברת עם המודל.
3. **בלי OCR ובלי חילוץ טקסט מ־PDF.** עברית נשברת בשתי הדרכים — כתב יד לא מזוהה,
   ו־PDF עברי מחזיר טקסט הפוך ומשובש. כל עמוד מומר לתמונה ונשלח למודל ראייה.
4. **RTL מלא מהרגע הראשון.** `enforceRTL()` נקרא ברמת המודול של `app/_layout.tsx`.
5. הפונקציה מחזירה JSON בלבד, במבנה שמוגדר ב־`src/types/study.ts`.
6. הפרומפט למודל מצהיר במפורש: כל הפלט בעברית, אך ורק מהחומר שהועלה, בלי ידע
   חיצוני, בלי להמציא.
7. מגבלת הוצאה: ספירת קריאות בטבלה, ועצירה מעל תקרה חודשית.

## מבנה

```
app/                  מסכים (expo-router)
  _layout.tsx         RTL, טעינת פונטים, safe area, Stack
  index.tsx           בית — רשימת החומרים
  upload.tsx          העלאה — צילום או קובץ
  material/[id].tsx   תוצאה — סיכום / כרטיסיות / קוויז
design/               קובץ העיצוב — המקור למערכת העיצוב
site/                 אתר הנחיתה הסטטי (Vercel)
supabase/
  migrations/         הסכימה
  functions/analyze   קובץ → מודל ראייה → JSON
  functions/materials קריאת חומרים
  functions/_shared   פרומפט, סכימה, DB, תקרה
src/
  components/         Screen, AppText, Rule, Chevron, Grain, PrimaryButton,
                      MaterialRow, EmptyState, TabBar, Flashcards, Quiz
  config/env.ts       משתני סביבה ציבוריים + אכיפת כלל ברזל 1
  data/materials.ts   שכבת נתונים — Supabase, עם מצב מקומי לפיתוח
  lib/                api, device, rtl, פורמט תאריכים
  theme/              tokens (צבעים, מרווחים, טיפוגרפיה), fonts
  types/              material, study (החוזה עם השרת)
scripts/              generate-grain.mjs — מייצר את assets/grain.png
```

## איך זרימת ההעלאה עובדת

1. `app/upload.tsx` קורא את הקובץ ל-base64 (`expo-file-system`) ושולח ל-`analyze`
2. `analyze` בודקת את התקרה החודשית, פותחת שורה במצב `processing`, ומחזירה 202
3. ברקע: הקובץ נשלח למודל ראייה — PDF כבלוק `document`, צילום כבלוק `image`
4. התשובה מאומתת מול החוזה ונשמרת. מצב החומר הופך ל-`ready` או ל-`failed`
5. מסך הבית מציג "בעיבוד" ומתעדכן בכניסה למסך או במשיכה למטה

המבנה נאכף פעמיים: Structured Outputs מכריח את סכימת ה-JSON בזמן הדגימה,
ו-`parseStudySet` בודק את התוכן — מערך ריק, מחרוזת ריקה, או `correct` מחוץ
לטווח לא מגיעים לתלמיד.

## עיצוב

מערכת העיצוב יושבת ב־`design/`, וכל הערכים שלה מיוצגים ב־`src/theme/tokens.ts`.
צבע או גודל חדש נכנס לטוקנים, לא לתוך מסך בודד. הפירוט המלא, כולל ההבדלים
המכוונים בין האתר לאפליקציה, ב־`design/README.md`.

## הערות RTL

- `I18nManager.forceRTL(true)` ב־native נכנס לתוקף אחרי רילוד אחד. לכן כל הסטיילים
  מצהירים במפורש על `textAlign: 'right'` ו־`writingDirection: 'rtl'`, ושורות
  משתמשות ב־`rowDirection` — כך הממשק נכון גם בהרצה הראשונה.
- חצים מצוירים ב־`Chevron`, לא נכתבים כתווים. תווים כמו `‹` הם bidi-mirrored
  ומתהפכים לבד לפי כיוון הפסקה.
- מחרוזת שמתחילה במילה לטינית (למשל "PDF או צילום") מקבלת כיוון פסקה LTR
  ומילותיה מסתדרות הפוך. כל טקסט מתחיל במילה עברית.
- משקלים דרך `fontFamily` ולא `fontWeight` — ב־Android משקל לא עובד על פונט
  שנטען כמשפחה נפרדת.

## השלבים הבאים

| # | מה |
|---|---|
| — | לפנות מקום ב-Supabase, לפרוס, ולבדוק העלאה אמיתית מקצה לקצה |
| 6 | חיבור `site/` לוורסל + רשימת המתנה אמיתית |
| 7 | אייקון, TestFlight |

לא בגרסה הזאת: התחברות, קהילה, שיתוף, הקלטות, פרסומות, חזרות מתוזמנות.
