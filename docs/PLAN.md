# LamdAI — ארכיטקטורה ותוכנית מימוש

מסמך שלב 1. אין כאן קוד אפליקציה — יש כאן את ההחלטות שהקוד ייבנה עליהן.
כל שלב במימוש חוזר לכאן, ומה שמשתנה בדרך מתעדכן כאן ולא נשאר בראש של מישהו.

---

## 1. מה המוצר, בשורה אחת

התלמיד מעלה חומר → המערכת מעבדת אותו פעם אחת → נוצרים סיכום, כרטיסיות,
קוויז ומאגר שאלות למבחן → התלמיד לומד, נבחן, ורואה במה הוא חלש.

הערך הוא **חומר ← הבנה ← תרגול ← תוצאות**, לא צ'אט.

---

## 2. סיכונים, לפני הארכיטקטורה

מסודר לפי כמה זה יכול להכאיב, לא לפי סדר הופעה.

### 2.1 עלות המודל היא העלות היחידה שגדלה עם משתמשים — ⚠️ קריטי

התשתית כולה חינם בהתחלה. המודל לא. כל העלאה עולה כסף אמיתי במוצר שהוא חינמי
לגמרי, כלומר אין שום דבר שמקזז את זה. תלמיד אחד שמעלה 40 עמודים ביום הוא
חשבון של עשרות דולרים בחודש.

**מה עושים:** עיבוד אחד לכל חומר, אף פעם לא שני. תקרה חודשית גלובלית שעוצרת
הכול. העלאה ראשונה חינם, ואחריה יחידות שנקנו — נגרעות בשרת לפני כל קריאה
למודל. מגבלת גודל וכמות עמודים לקובץ. הפירוט המספרי בסעיף 8.

### 2.2 עברית שוברת חילוץ טקסט — ⚠️ קריטי, משפיע על כל הפייפליין

הספק בפרומפט מדבר על `EXTRACT CONTENT → CLEAN CONTENT`. בעברית זה לא עובד כמו
באנגלית, משתי סיבות בלתי תלויות:

1. **PDF עברי מחזיר טקסט הפוך ומשובש.** רוב מחוללי ה-PDF כותבים עברית
   בסדר ויזואלי ולא לוגי, בלי סימון bidi אמין. `pdf-parse` ודומיו יחזירו
   מחרוזות שנראות תקינות ומשמעותן הפוכה. זה כשל שקט — לא נזרקת שגיאה,
   פשוט מגיע זבל למודל.
2. **דף מחברת בכתב יד הוא המקרה הנפוץ אצל תלמידי תיכון, ואין ממנו טקסט בכלל.**
   OCR בעברית בכתב יד הוא לא בעיה פתורה.

**מה עושים (ההמלצה שלי):** בלי OCR ובלי חילוץ טקסט. כל עמוד נשלח כמו שהוא
למודל ראייה — PDF כבלוק `document`, צילום כבלוק `image`. המודל רואה את הדף,
לא מחרוזת ששוחזרה ממנו. זה גם עולה יותר בטוקנים וגם יותר טוב, וההפרש באיכות
בעברית הוא לא שולי. **זו שאלה פתוחה שאני מבקש עליה החלטה** — ראה סעיף 13.

### 2.3 העיבוד ארוך מ-timeout של פונקציה — גבוה

קריאה למודל על 6 עמודים סרוקים לוקחת עשרות שניות עד דקות. Vercel Hobby חותך
פונקציות ב-60 שניות. פונקציה סינכרונית שמחזירה את הסיכום היא ארכיטקטורה
שתישבר בייצור על החומר הראשון שהוא לא דף אחד.

**מה עושים:** העיבוד אסינכרוני. ה-API מחזיר מיד `study_set` במצב `queued`,
עובד רקע מעבד, והלקוח מקבל עדכוני מצב אמיתיים. פירוט בסעיף 6.

### 2.4 גבול גודל הבקשה של Vercel — גבוה

Vercel חוסם גוף בקשה של יותר מ-4.5MB ל-Serverless Function. PDF של סריקה
עובר את זה בקלות. העלאה שעוברת דרך `/api` תיפול על קבצים אמיתיים.

**מה עושים:** הלקוח מעלה **ישירות ל-Supabase Storage** מול signed upload URL
שנוצר בשרת. הקובץ לא עובר דרך Vercel בכלל.

### 2.5 קהל היעד הוא קטינים — גבוה, לא טכני

בני 13–18. זה נוגע בהגנת הפרטיות (תיקון 13 לחוק הגנת הפרטיות שנכנס לתוקף
ב-2025), במדיניות של Google OAuth לחשבונות מנוהלים של בתי ספר, ובתנאי השימוש
של ספק המודל לגבי משתמשי קצה קטינים.

**מה עושים בקוד:** מינימום מידע אישי (אימייל + שם תצוגה בלבד, בלי גיל,
בלי בית ספר, בלי כיתה), מחיקת קובץ המקור אחרי עיבוד מוצלח, ומסלול מחיקת חשבון
שמוחק הכול. **מה שאני לא יכול לעשות:** לכתוב לך מדיניות פרטיות ותנאי שימוש
שעומדים בדין. זה צריך עורך דין לפני שאתה פותח את זה לתלמידים אמיתיים, לא
אחרי. אני מסמן ולא חוסם.

### 2.6 המודל ממציא, או מסרב — בינוני

חומר לא קריא, דף חתוך, צילום מטושטש. המודל עלול להשלים מידע חיצוני ולייצר
כרטיסייה נכונה עובדתית שלא נמצאת בחומר — וזה בדיוק מה שהורס אמון של תלמיד
שנבחן על מה שהמורה נתן.

**מה עושים:** הפרומפט אוסר ידע חיצוני במפורש; Structured Outputs מכריח את
סכימת ה-JSON בזמן הדגימה; ולידציה בשרת על התוכן עצמו (מערך ריק, מחרוזת ריקה,
`correct` מחוץ לטווח) לפני שמירה; `stop_reason: "refusal"` מטופל כשגיאה
ידידותית בעברית. שגיאה גולמית של המודל אף פעם לא מגיעה למסך.

### 2.7 RTL הוא לא `dir="rtl"` — בינוני

עברית טהורה עובדת. מה שנשבר: מחרוזת שמתחילה במילה לטינית מקבלת כיוון פסקה
LTR והמילים מסתדרות הפוך; חצים שנכתבים כתווים (`‹`) מתהפכים לבד; נוסחאות
מתמטיקה ומונחים באנגלית בתוך משפט עברי; סרגלי התקדמות שממלאים מהצד הלא נכון.

**מה עושים:** Tailwind logical properties בלבד (`ms-*`/`me-*`/`ps-*`/`pe-*`,
`start-*`/`end-*`) — אף פעם לא `ml-*`/`pl-*`. חצים כאייקוני SVG. כל מחרוזת
בממשק מתחילה במילה עברית. בדיקה ידנית של כל מסך ב-360px.

### 2.8 השאר — נמוך, מסומן ומטופל

| סיכון                                             | טיפול                                                                         |
| ------------------------------------------------- | ----------------------------------------------------------------------------- |
| Vercel Hobby אסור לשימוש מסחרי לפי התנאים         | חינם זה בסדר עכשיו; רגע לפני מונטיזציה — Pro ב-$20                            |
| פרויקט Supabase חינמי נעצר אחרי 7 ימי חוסר פעילות | בפיתוח זה מעצבן ולא נורא; לפני משתמשים אמיתיים — Pro ב-$25                    |
| מכסת Storage חינמית 1GB                           | מחיקת קובץ המקור 24 שעות אחרי עיבוד מוצלח (סעיף 7)                            |
| העלאת דפי ספר לימוד מוגן בזכויות יוצרים           | שימוש אישי, בלי שיתוף ובלי הפצה; הקובץ נמחק                                   |
| מפתח המודל דולף                                   | הוא קיים רק ב-Secrets של עובד הרקע; אף פעם לא ב-`NEXT_PUBLIC_*` ולא בקוד לקוח |

---

## 3. ארכיטקטורה

```
┌────────────────────────────────────────────────────────┐
│  דפדפן (mobile-first, RTL)                            │
│  Next.js App Router · React Server Components          │
└───────┬─────────────────────┬──────────────────────────┘
        │ העלאה ישירה          │ קריאה/כתיבה מאומתת
        │ (signed URL)         │
        ▼                      ▼
┌──────────────────┐   ┌──────────────────────────────────┐
│ Supabase Storage │   │ Next.js (Vercel)                 │
│ bucket פרטי      │   │ · Auth callback + middleware     │
└────────┬─────────┘   │ · Server Actions / Route Handlers│
         │             │ · service-role לפעולות מיוחסות   │
         │             └───────────────┬──────────────────┘
         │                             │ קריאה (service role)
         │                             ▼
         │             ┌──────────────────────────────────┐
         │             │ Supabase Postgres · RLS על הכול  │
         │             └───────────────▲──────────────────┘
         │ מוריד את הקובץ               │ כותב תוצאות
         ▼                             │
┌────────────────────────────────────────────────────────┐
│ Supabase Edge Function: process-material  (Deno)       │
│ העובד היחיד שמדבר עם המודל. רץ ברקע, בלי timeout של 60ש׳│
└────────────────────────┬───────────────────────────────┘
                         ▼
                 ┌───────────────┐
                 │ Anthropic API │
                 └───────────────┘
```

**למה העובד יושב ב-Supabase ולא ב-Vercel.** Vercel Hobby חותך ב-60 שניות,
ועיבוד של PDF סרוק עובר את זה. Edge Function של Supabase ממשיכה ברקע דרך
`EdgeRuntime.waitUntil` אחרי שהיא כבר החזירה תשובה, היא רצה באותו ענן שבו
יושבים ה-DB וה-Storage (הקובץ לא חוצה רשתות), והמפתח של המודל חי ב-Secrets
שלה בלבד. אפס תשתית נוספת, אפס תור, אפס Redis.

**עקרונות שלא נשברים:**

1. המודל נקרא **רק** מ-`process-material`. לא מהדפדפן, לא מ-Route Handler.
2. עיבוד אחד לכל חומר. פתיחת סיכום, כרטיסיות, קוויז או מבחן — קריאות DB בלבד.
3. אין `SUPABASE_SERVICE_ROLE_KEY` ואין מפתח מודל בשום דבר שמתחיל ב-`NEXT_PUBLIC_`.
4. הרשאה נאכפת ב-RLS בבסיס הנתונים. הבדיקה בפרונט היא UX, לא אבטחה.
5. ספק המודל מאחורי ממשק אחד (סעיף 6.4). החלפת מודל היא משתנה סביבה.

---

## 4. סכימת בסיס הנתונים

UUIDs, מפתחות זרים עם `on delete cascade`, `created_at`/`updated_at` בכל טבלה
שמתעדכנת, אינדקס על כל FK שנשאל דרכו.

### 4.1 היחסים

```
auth.users ─1:1─ profiles
     │
     └─1:N─ study_sets ──1:N─ documents        (הקבצים שהועלו)
                │
                ├──1:N─ topics                 (הנושאים שזוהו)
                ├──1:1─ summaries              (סיכום + נקודות + הגדרות)
                ├──1:N─ flashcards ──1:N─ flashcard_reviews
                ├──1:N─ questions              (kind: quiz | exam)
                └──1:N─ attempts ──1:N─ attempt_answers
```

### 4.2 הטבלאות

| טבלה                | תפקיד                                 | שדות עיקריים                                                                                   |
| ------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `profiles`          | הרחבה של `auth.users`                 | `id` (FK ל-auth.users), `display_name`, `created_at`                                           |
| `study_sets`        | יחידת הלימוד. מה שהתלמיד רואה בדשבורד | `user_id`, `title`, `subject`, `status`, `stage`, `error`, `page_count`                        |
| `documents`         | קובץ שהועלה                           | `study_set_id`, `user_id`, `storage_path`, `mime_type`, `size_bytes`, `checksum`, `deleted_at` |
| `topics`            | נושא שזוהה בחומר                      | `study_set_id`, `name`, `order_index`                                                          |
| `summaries`         | 1:1 עם study_set                      | `study_set_id`, `body`, `key_points jsonb`, `definitions jsonb`                                |
| `flashcards`        | כרטיסייה                              | `study_set_id`, `topic_id`, `front`, `back`, `order_index`                                     |
| `flashcard_reviews` | משוב התלמיד                           | `user_id`, `flashcard_id`, `rating` (0/1/2), `created_at`                                      |
| `questions`         | שאלה אמריקאית                         | `study_set_id`, `topic_id`, `kind`, `stem`, `options jsonb`, `correct_index`, `explanation`    |
| `attempts`          | ניסיון קוויז או מבחן                  | `user_id`, `study_set_id`, `kind`, `question_count`, `score`, `started_at`, `finished_at`      |
| `attempt_answers`   | תשובה בודדת בניסיון                   | `attempt_id`, `question_id`, `selected_index`, `is_correct`                                    |
| `model_calls`       | כל קריאה למודל, כולל כשלונות          | `user_id`, `study_set_id`, `model`, `input_tokens`, `output_tokens`, `cost_usd`, `ok`, `error` |
| `usage_caps`        | שורה אחת, תקרה גלובלית                | `max_calls_per_month`, `max_cost_usd_per_month`, `max_uploads_per_user_per_month`              |

12 טבלאות. הספק הציע 11 ואמר "בלי טבלאות מיותרות" — שתי החלטות שמצמצמות:

- **`questions` אחת במקום `quiz_questions` + `exam_questions`.** המבנה זהה
  לחלוטין; `kind` מבדיל. שתי טבלאות תאומות זה כפילות בסכימה, ב-RLS ובקוד.
- **`attempts` אחת במקום `quiz_attempts` + `exam_attempts`.** אותו נימוק.

`model_calls` ו-`usage_caps` הן תשתית עלות, לא מודל המוצר — הן מעבר ל-11 של
הספק בכוונה, כי סעיף 2.1 מחייב אותן.

### 4.3 שליטה, לא טבלה

`topic_mastery` הוא **VIEW**, לא טבלה:

```sql
-- למען הבהירות; הנוסח המדויק ייכתב בשלב 12
create view topic_mastery as
  select user_id, study_set_id, topic_id,
         <שקלול של flashcard_reviews ושל attempt_answers> as mastery
  ...
```

טבלה מחושבת חייבת סנכרון, וסנכרון נשבר. VIEW לא יכול להיות לא מסונכרן. אם
בעתיד זה יהיה איטי — `materialized view` עם רענון, וזו החלפה מקומית.

**נוסחת המאסטרי (V1, פשוטה בכוונה):**
`mastery = 0.6 × אחוז נכונות בשאלות הנושא + 0.4 × ממוצע דירוגי הכרטיסיות (0/0.5/1)`,
כשיש נתונים משני הצדדים; אחרת מה שיש. בלי חזרות מרווחות בגרסה הזאת, לפי הספק.

### 4.4 RLS

RLS פעיל על כל טבלה. שני דפוסים בלבד:

```sql
-- טבלה עם user_id
create policy own_rows on <t> for select using (auth.uid() = user_id);

-- טבלת ילד — דרך ה-study_set
create policy own_via_set on <t> for select using (
  exists (select 1 from study_sets s
          where s.id = <t>.study_set_id and s.user_id = auth.uid())
);
```

**מה שהלקוח לא יכול לכתוב בכלל:** `topics`, `summaries`, `flashcards`,
`questions`, `model_calls`, `usage_caps`. אין להן policy של `insert`/`update`
בכלל — רק העובד, שרץ עם service role ועוקף RLS, כותב לשם. תוכן שנוצר מהמודל
לא ניתן לעריכה מהדפדפן, נקודה.

**מה שכן:** `attempts`, `attempt_answers`, `flashcard_reviews` — insert של
המשתמש על השורות שלו. `profiles` — update על השורה שלו.

`grant`-ים לתפקידי `anon`/`authenticated` נסגרים במפורש על כל טבלה שלא צריכה
אותם. RLS בלי שלילת grants הוא חצי נעילה.

---

## 5. מבנה התיקיות

הוקם בשלב 2 עם `create-next-app` — Next.js 16.3, React 19.2, Tailwind v4.

```
src/
├─ app/
│  ├─ layout.tsx                  <html lang="he" dir="rtl">, פונטים, מטא־דאטה
│  ├─ globals.css                 מערכת העיצוב — @theme של Tailwind v4
│  ├─ page.tsx                    דף הנחיתה                          ✓ שלב 2
│  ├─ (auth)/
│  │  ├─ login/page.tsx
│  │  ├─ signup/page.tsx
│  │  └─ callback/route.ts        החלפת code ל-session
│  ├─ (app)/                      אזור מוגן — layout מוודא session
│  │  ├─ layout.tsx
│  │  ├─ dashboard/page.tsx
│  │  ├─ upload/page.tsx
│  │  └─ sets/[id]/
│  │     ├─ page.tsx              סיכום (ברירת מחדל)
│  │     ├─ processing/page.tsx   שלבי עיבוד אמיתיים
│  │     ├─ flashcards/page.tsx
│  │     ├─ quiz/page.tsx
│  │     ├─ exam/page.tsx
│  │     └─ results/[attemptId]/page.tsx
│  └─ api/
│     ├─ uploads/route.ts         מחזיר signed upload URL
│     ├─ study-sets/route.ts      יוצר רשומה ומעיר את העובד
│     └─ attempts/route.ts        פותח וסוגר ניסיון, מנקד בשרת
├─ components/
│  ├─ ui/                         shadcn — ללא שינוי ידני
│  └─ study/                      Flashcard, QuestionCard, ScoreRing, Stages
├─ lib/
│  ├─ brand.ts                    שם המוצר, במקום אחד                ✓ שלב 2
│  ├─ supabase/{browser,server,admin}.ts
│  ├─ ai/{provider,anthropic,schema,prompt}.ts
│  ├─ validation/                 סכימות zod לכל קלט חיצוני
│  └─ mastery.ts
└─ proxy.ts                       רענון session + חסימת מסלולים

supabase/
├─ migrations/                    מקור האמת לסכימה
└─ functions/process-material/    העובד (Deno)

design/                           קובץ העיצוב — המקור למערכת העיצוב
docs/PLAN.md                      המסמך הזה
```

### מלכודת של Next 16 ששווה לזכור

**`middleware.ts` הוחלף ב-`proxy.ts`.** הפונקציה המיוצאת נקראת `proxy` ולא
`middleware`, והיא רצה על Node.js ולא על Edge. קובץ בשם `middleware.ts`
פשוט לא ירוץ — בלי שגיאה, בלי אזהרה, ועם הגנת מסלולים שלא קיימת. זה מתועד
גם ב-`AGENTS.md` כי זה בדיוק סוג הכשל שחוזר.

## 6. פייפליין ה-AI

### 6.1 הזרימה

```
1. הלקוח מבקש signed upload URL         POST /api/uploads
                                        השרת: מאמת session, מכסה, סוג, גודל
2. הלקוח מעלה ישירות ל-Storage           PUT → supabase storage
                                        (לא עובר דרך Vercel — סעיף 2.4)
3. הלקוח פותח study_set                 POST /api/study-sets
                                        השרת: יוצר שורה status=queued,
                                        קורא ל-process-material, מחזיר id
4. העובד מוריד את הקובץ                  stage=reading
5. מאמת magic bytes                     לא סומכים על הסיומת
6. קריאה אחת למודל ראייה                 stage=analyzing
7. ולידציה על התוצאה                     stage=writing
8. כתיבה בטרנזקציה אחת                   topics, summaries, flashcards,
                                        questions(quiz+exam)
9. status=ready                          stage=done
```

מכאן והלאה: **אפס קריאות למודל**. סיכום, כרטיסיות, קוויז, מבחן — הכול
`select` מהטבלאות.

### 6.2 מסך העיבוד לא משקר

עמודת `stage` מתעדכנת על ידי העובד בזמן אמת, והמסך מציג את השלב שבאמת רץ:

| stage       | מה מוצג                             |
| ----------- | ----------------------------------- |
| `queued`    | "החומר התקבל"                       |
| `reading`   | "קורא את הדפים..."                  |
| `analyzing` | "מנתח את החומר ומסדר את הנושאים..." |
| `writing`   | "כותב את הסיכום ובונה שאלות..."     |
| `done`      | מעבר אוטומטי למסך החומר             |
| `failed`    | הודעה בעברית + כפתור "נסה שוב"      |

הלקוח מאזין ב-Supabase Realtime על השורה. Realtime כלול בחינם ואין בו polling.
Polling כל 3 שניות הוא fallback אם המנוי נופל.

**אין סרגל התקדמות באחוזים.** אי אפשר לדעת כמה נשאר, ומספר שרץ ל-90% ונתקע
זה שקר שהתלמיד מזהה.

### 6.3 הפלט

קריאה אחת מחזירה JSON מאולץ סכימה:

```jsonc
{
  "subject": "ביולוגיה",
  "title": "מערכת העיכול",
  "topics": ["אנזימי עיכול", "ספיגה במעי", "..."],
  "summary": "טקסט מסודר בפסקאות",
  "key_points": ["...", "..."],
  "definitions": [{ "term": "...", "meaning": "..." }],
  "flashcards": [{ "q": "...", "a": "...", "topic": "אנזימי עיכול" }],
  "quiz_questions": [
    {
      "q": "...",
      "options": ["", "", "", ""],
      "correct": 0,
      "explanation": "...",
      "topic": "ספיגה במעי",
    },
  ],
  "exam_questions": [/* אותו מבנה, ~36 שאלות */],
}
```

**למה מאגר מבחן נוצר מראש ולא לפי דרישה:** אנחנו לא שומרים את הטקסט של החומר
(סעיף 2.2 — אין חילוץ טקסט). כלומר קריאה שנייה תחייב לשלוח את ה-PDF שוב
ולשלם על הקלט מחדש. עדיף לשלם פעם אחת על פלט ארוך יותר. המבחן דוגם 10/20/30
מתוך ~36, ולכן שני מבחנים לא זהים.

הסכימה נאכפת **פעמיים**: `output_config.format` מכריח אותה בזמן הדגימה, ופונקציית
`parseStudySet` בודקת את התוכן עצמו לפני שמירה. מערך ריק, מחרוזת ריקה או
`correct` מחוץ לטווח לא מגיעים לתלמיד.

### 6.4 הפשטת הספק

```ts
// lib/ai/provider.ts
export interface StudySetGenerator {
  generate(file: FileInput): Promise<{ studySet: StudySet; usage: Usage }>;
}
```

מימוש אחד — `anthropic.ts`. המודל נבחר במשתנה סביבה `LAMDAI_MODEL`, לא בקוד.
החלפת ספק = קובץ חדש שמממש את הממשק. אין `if (provider === ...)` בשום מקום אחר.

### 6.5 ניקוד — מי מחזיק את התשובות

|           | הלקוח מקבל `correct_index`? | ניקוד                        |
| --------- | --------------------------- | ---------------------------- |
| **קוויז** | כן                          | בלקוח, מיידית                |
| **מבחן**  | לא                          | בשרת, ב-`POST /api/attempts` |

הקוויז הוא כלי לימוד — משוב מיידי חשוב יותר מהגנה, והרמאות היחידה האפשרית היא
בתלמיד עם עצמו. המבחן מדמה מבחן, והציון בו אמור להיות אמין, ולכן התשובות
הנכונות לא עוזבות את השרת עד שהוא נסגר.

---

## 7. אחסון

- Bucket `materials`, **פרטי**. אין קריאה ציבורית.
- נתיב: `{user_id}/{study_set_id}/{uuid}.{ext}` — policy מוודאת שהמקטע הראשון
  בנתיב שווה ל-`auth.uid()`.
- הגבלות: 15MB לקובץ, 20 עמודים, `application/pdf` / `image/jpeg` / `image/png`.
- **הסיומת לא נבדקת. ה-magic bytes נבדקים** בעובד לפני שהקובץ נשלח למודל.
- קריאה רק דרך signed URL קצר-מועד.
- **מחיקה: קובץ המקור נמחק 24 שעות אחרי עיבוד מוצלח.** אין בו צורך — ה-study set
  שמור. זה חוסך את מכסת ה-1GB, ומקטין את החשיפה כשמדובר בחומר של קטינים.

---

## 7א. המודל העסקי — יחידות העלאה

**ההעלאה הראשונה חינם. אחריה קונים יחידות.** יחידה אחת = העלאת חומר
אחד. מה שנוצר ממנו — סיכום, כרטיסיות, תרגול ומבחן — נשאר פתוח בלי
הגבלה ובלי תשלום נוסף.

| חבילה    | יחידות | מחיר | ליחידה |
| -------- | ------ | ---- | ------ |
| מבחן אחד | 5      | ₪29  | ₪5.8   |
| מחצית    | 15     | ₪69  | ₪4.6   |
| בגרות    | 40     | ₪149 | ₪3.7   |

### למה יחידות ולא מנוי חודשי

1. **ככה נראית העלות שלנו.** אנחנו משלמים לכל העלאה (~₪1.75 ב-Opus 5),
   לא לכל חודש. מחיר שמתנהג כמו העלות לא יכול להפסיד על משתמש כבד.
2. **ככה נראה השימוש.** תלמיד מעלה שמונה חומרים בשבוע שלפני מבחן, ואז
   שבועיים כלום. מנוי חודשי גובה בשבועות השקטים — ומבוטל — וחוסם דווקא
   בשבוע העמוס, שהוא הרגע היחיד שבו הוא באמת צריך אותנו.
3. **ככה נראית העונה.** אחרי הבגרות בקיץ אין שימוש בכלל. מנוי במוצר
   עונתי הוא מנוי שמבוטל, ואצל תלמידי תיכון הביטול עובר דרך ההורה
   שרואה חיוב חוזר ושואל מה זה.
4. **ככה קונים בני נוער בישראל.** ביט הוא העברה חד-פעמית. מנוי דורש
   כרטיס אשראי של הורה והסכמה מתמשכת; חבילה דורשת החלטה אחת.
5. **זה מוחק את תלונת המתחרים.** התלונה החוזרת על Turbo היא חיוב מפתיע
   וחידוש שלא ביקשו. אין לנו חידוש, ולכן אין תלונה.

### מה שזה עולה לנו, ומה נשאר

עלות של ~₪1.75 להעלאה. החבילה הקטנה מחזירה פי 3.3, הגדולה פי 2.1. כל
מכירה רווחית ביחידה בלי תלות בדפוס השימוש. הבדיקה הזאת רצה אוטומטית
ב-`src/lib/__tests__/redeem.test.ts`: מחיר ליחידה חייב להישאר מעל פי
שניים מהעלות.

### מה שהחלטנו לא לגבות עליו

**המבחן נפתח לכולם.** השאלות כבר נוצרו בעיבוד היחיד ופתיחת מבחן היא
קריאת DB בלבד — אפס עלות. לגבות עליה זה לגבות פעמיים על חומר ששולם.

### איך זה בנוי

- יתרה: `profiles.upload_credits`. יומן מלא ב-`credit_events` — כל זיכוי
  וכל גריעה, כדי שיהיה מענה ל"למה נגמרו לי היחידות".
- **הגריעה היא באותה טרנזקציה של יצירת החומר**, בטריגר על `study_sets`,
  עם `update ... where upload_credits > 0`. בדיקה ואז גריעה בשתי פקודות
  היא מרוץ ששתי העלאות מקבילות עוברות בו על יחידה אחת.
- **עיבוד שנכשל מחזיר את היחידה** — טריגר על מעבר ל-`error`, פעם אחת
  (`credit_refunded`). תלמיד לא משלם על כשל שלנו.
- מחירי החבילות ב-`credit_packs` במסד ומוצגים מ-`src/lib/pricing.ts`.
  שינוי מחיר נעשה בשני המקומות.
- הסליקה: PayPlus, דף תשלום מאורח. הזיכוי קורה **רק ב-callback חתום
  שרת-לשרת** (`/api/payments/payplus/callback`), אף פעם לא בחזרת המשתמש
  לדף ההצלחה. רישום וזיכוי בטרנזקציה אחת עם `unique` על מזהה העסקה:
  callback כפול לא מזכה פעמיים.
- `grant_credits` פתוחה ל-service role בלבד.

### מכירה ידנית וקודי הפעלה — מסלול הביניים

עד שהסליקה חיה, הרכישה נעשית בשיחה: התלמיד לוחץ "לכתוב לי על חבילה"
(`NEXT_PUBLIC_CONTACT_URL` — וואטסאפ, מייל, מה שנוח), מעביר תשלום בביט,
ומקבל קוד. הוא מקליד אותו ב-`/premium` והיחידות נכנסות.

ייצור קודים, מ-SQL Editor של Supabase:

```sql
select * from public.create_redeem_codes('term', 5, 'ביט — כיתה יב תלפיות');
--                                        ^חבילה ^כמה ^למי, לזיכרון שלך
```

- **לתלמיד אין גישת קריאה לטבלת הקודים.** RLS דלוקה בלי אף policy, ולכן
  היחיד שרואה קודים הוא service role. select פתוח על טבלה כזאת הוא
  רשימת יחידות בחינם לכל מי שפותח קונסול.
- **המימוש אטומי** — `for update` על השורה. שתי לחיצות לא מזכות פעמיים.
- כל מימוש נרשם ב-`payments` עם `provider = 'manual'` וסכום 0.

**מה שזה לא:** אין חשבונית ואין גבייה אוטומטית. המעקב אחרי מי שילם הוא
ידני. זה מסלול שמאפשר למכור עכשיו, לא לתמיד.

### מה שעוד לא נסגר

המתאם ל-PayPlus נכתב מול התיעוד ולא נבדק מול חשבון אמיתי — אין עדיין
מפתחות. שלושת הדברים שצריך לאמת לפני ייצור מפורטים בראש
`src/lib/payments/payplus.ts`.

**מה שנבדוק אחרי המכירות הראשונות:** אם יותר משליש מהקונים קונים שוב
תוך חודש — יש מקום גם למנוי לכבדים, לצד החבילות ולא במקומן.

**החלטות שהוחלפו:** טבלת `plans` ומסלול `premium` נשארו במסד אבל אינם
בשימוש — `can_create_study_set` ו-`my_entitlements` עובדות מול היתרה.

---

## 8. עלויות

### 8.1 תשתית

| שירות              | היום                                    | כשיש משתמשים אמיתיים                          |
| ------------------ | --------------------------------------- | --------------------------------------------- |
| Vercel             | Hobby — $0                              | Pro — $20/חודש (וגם: Hobby אסור לשימוש מסחרי) |
| Supabase           | Free — $0 (נעצר אחרי 7 ימי חוסר פעילות) | Pro — $25/חודש                                |
| Google Cloud OAuth | $0                                      | $0                                            |
| דומיין             | ~$12/שנה                                | ~$12/שנה                                      |
| Sentry / PostHog   | Free — $0                               | Free — $0                                     |
| **סה"כ תשתית**     | **$0**                                  | **~$46/חודש**                                 |

### 8.2 המודל — זה מה שקובע

מחירון לכל מיליון טוקנים (קלט/פלט), נכון ליוני 2026: Opus 5 — $5/$25 ·
Sonnet 5 — $2/$10 · Haiku 4.5 — $1/$5.

אומדן להעלאה טיפוסית — PDF של 6 עמודים דרך ראייה (~13K טוקני קלט, כ-2.2K
לעמוד) ופלט מלא כולל מאגר מבחן (~16K טוקני פלט, כולל חשיבה):

| מודל      | להעלאה טיפוסית | בשקלים | להעלאה מקסימלית (20 עמודים) |
| --------- | -------------- | ------ | --------------------------- |
| Opus 5    | ~$0.47         | ~₪1.75 | ~$0.62 · ~₪2.30             |
| Sonnet 5  | ~$0.19         | ~₪0.70 | ~$0.25 · ~₪0.95             |
| Haiku 4.5 | ~$0.09         | ~₪0.35 | ~$0.13 · ~₪0.50             |

הפלט הוא הרוב המכריע של העלות — 86% ממנה בהעלאה טיפוסית. לכן מספר
העמודים משפיע פחות משנדמה, ואורך הפלט (כמות השאלות והכרטיסיות) משפיע
יותר.

**מה שזה אומר על החבילות** (סעיף 7א), במקרה הגרוע של 20 עמודים לכל
העלאה ובמודל היקר:

| חבילה | הכנסה | עלות במקרה הגרוע | נשאר |
| ----- | ----- | ---------------- | ---- |
| מבחן  | ₪29   | ₪11.5            | 60%  |
| מחצית | ₪69   | ₪34.5            | 50%  |
| בגרות | ₪149  | ₪92              | 38%  |

חבילת הבגרות היא הדקה מכולן, ובכוונה — היא גם זו שנקנית הכי פחות.
אם היא תהפוך לנפוצה, המחיר שלה צריך לעלות או שהיא עוברת ל-Sonnet.

**עלות שלא נראית בטבלה:** קריאה שנכשלה אחרי שהמודל כבר עבד עולה כסף
מלא, ומחזירה יחידה לתלמיד (סעיף 7א). כלומר כשל אחד עולה פעמיים. זו
הסיבה שכל קריאה נרשמת, כולל כשלונות.

**מה שלא יעזור כאן:** Batch API נותן 50% הנחה אבל עד 24 שעות המתנה — תלמיד
לא מחכה יום. Prompt caching לא עוזר כי כל חומר שונה, והפרומפט הקבוע קצר מדי
מסף המטמון. הדרך היחידה להוזיל היא לבחור מודל זול יותר או לייצר פחות פלט.

### 8.3 הבלמים

1. תקרה חודשית גלובלית ב-`usage_caps`. מעליה — 429 והודעה בעברית. משנים אותה
   ב-SQL בלי לפרוס מחדש.
2. מכסת משתמש: חומר אחד בחינם, ואחריו יחידות שנקנו. הגריעה בטריגר,
   באותה טרנזקציה של יצירת החומר — ראה סעיף 7א. ב-`usage_caps` נשארה
   תקרה אישית של 60 בחודש כבלם נגד שימוש חריג בלבד.
3. **כל קריאה נרשמת, כולל כשלונות** — קריאה שנפלה אחרי שהמודל כבר עבד עלתה כסף.
4. 15MB ו-20 עמודים לקובץ.

---

## 9. אבטחה

| שכבה          | מה                                                                         |
| ------------- | -------------------------------------------------------------------------- |
| אימות         | Supabase Auth, cookies של httpOnly, `middleware.ts` מרענן ומגן על `(app)/` |
| הרשאה         | RLS על כל טבלה; תוכן שנוצר מהמודל אינו ניתן לכתיבה מהלקוח                  |
| קלט           | zod על כל גוף בקשה; אורך כותרת, סוג קובץ, גודל, מספר עמודים                |
| קבצים         | magic bytes, לא סיומת; bucket פרטי; signed URL בלבד                        |
| Rate limiting | מכסת משתמש ב-DB + תקרה גלובלית; הגבלת קצב על `/api/uploads`                |
| סודות         | `SUPABASE_SERVICE_ROLE_KEY` רק בשרת; מפתח המודל רק ב-Secrets של העובד      |
| שגיאות        | הודעה בעברית למשתמש; הפירוט ללוג בלבד                                      |
| בדיקה         | `supabase db lint` + advisors ב-CI, ובדיקה ידנית של גישה חוצת-משתמשים      |

**מבחן שחייב לעבור לפני ייצור:** שני חשבונות, A ו-B. A מנסה לקרוא כל טבלה
עם ה-`study_set_id` של B, ולהוריד את הקובץ של B. שניהם חייבים להיכשל
בבסיס הנתונים, לא בפרונט.

---

## 10. חשבונות ושירותים

### מה אני יכול לעשות

| מה                                          | איך                            |
| ------------------------------------------- | ------------------------------ |
| יצירת פרויקט Supabase (אזור פרנקפורט)       | דרך ה-MCP, אחרי אישור שלך      |
| סכימה, RLS, grants, hardening               | migrations + `apply_migration` |
| Bucket ומדיניות Storage                     | migration                      |
| פריסת `process-material`                    | `deploy_edge_function`         |
| כל קוד ה-Next.js, הבדיקות, ה-CI             | בריפו                          |
| `vercel.json`, הגדרות פריסה, `.env.example` | בריפו                          |
| בדיקת אבטחה מול advisors של Supabase        | MCP                            |

### מה אתה חייב לעשות בעצמך

| #   | מה                                                | איפה                     | למה לא אני                   |
| --- | ------------------------------------------------- | ------------------------ | ---------------------------- |
| 1   | חשבון Vercel וחיבור הריפו                         | vercel.com/new           | דורש התחברות והרשאה ל-GitHub |
| 2   | הדבקת משתני הסביבה ב-Vercel                       | Vercel → Settings → Env  | בקונסולה                     |
| 3   | פרויקט ב-Google Cloud + מסך הסכמה + OAuth Client  | console.cloud.google.com | מסך הסכמה ואימות זהות        |
| 4   | הדבקת Client ID/Secret ב-Supabase → Auth → Google | קונסולת Supabase         | סוד שאסור לעבור דרכי בקוד    |
| 5   | מפתח Anthropic + חיוב                             | console.anthropic.com    | אימות תשלום                  |
| 6   | `supabase secrets set ANTHROPIC_API_KEY=...`      | טרמינל אצלך              | המפתח לא עובר דרכי           |
| 7   | קניית דומיין וחיבורו ל-Vercel                     | רשם + Vercel             | תשלום                        |

לכל אחד מהשבעה אכתוב הוראות לחיצה מדויקות ברגע שנגיע אליו. אני לא עוקף
CAPTCHA, אימות טלפון, אימות זהות, אימות תשלום או מסכי הסכמה, ולא מבקש סיסמאות.

**החלטות שכבר קיבלתי כדי לא לעכב אותך:** אזור Supabase יהיה **eu-central-1
(פרנקפורט)** — ~50ms מישראל לעומת ~280ms מאזורים רחוקים, וזה ההבדל בין ממשק
שמרגיש מקומי לממשק שמרגיש עייף. לבחור אזור אפשר רק ביצירת הפרויקט.

---

## 11. תוכנית השלבים

| שלב  | מה                                                               | מוגדר "נגמר" כאשר                                                                                                               |
| ---- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 1    | ארכיטקטורה, סכימה, פייפליין, עלויות                              | **המסמך הזה** — ממתין לאישורך                                                                                                   |
| 2    | Next.js + TS + Tailwind + shadcn + ESLint + Prettier + מעטפת RTL | `npm run build` ו-`typecheck` נקיים; דף בעברית ב-360px                                                                          |
| 3 ✓  | Supabase: פרויקט, סכימה, RLS, grants, bucket                     | **הושלם.** פרויקט `ecokutjdqppemewxyvat` בפרנקפורט; advisors נקי מ-WARN ומ-ERROR; מבחן A/B עבר — הפירוט ב-`supabase/README.md`  |
| 4 ✓  | אימות: אימייל/סיסמה, proxy.ts, מסלולים מוגנים                    | **הושלם ואומת מקצה לקצה בדפדפן** — ראה 11.1. Google ממתין לפרטי OAuth                                                           |
| 5 ✓  | דשבורד + מצבים ריקים                                             | **הושלם ואומת.** רשימת חומרים אמיתית מה-DB, מצב ריק, ספירות ומאסטרי                                                             |
| 6 ✓  | העלאה: signed URL, ולידציה, התקדמות                              | **הושלם.** העלאה ישירה ל-Storage; עד 20 עמודים, 15MB לקובץ, 25MB סה"כ                                                           |
| 7 ◐  | `process-material` + מסך העיבוד                                  | **נפרס ופעיל.** מריץ, מאמת קלט ומגיע ל-DB. חסר רק `ANTHROPIC_API_KEY` להרצה אמיתית                                              |
| 8 ✓  | סיכום                                                            | **הושלם ואומת.** פסקאות, עיקרי דברים, מושגים ונושאים — נבדק ב-390px                                                             |
| 9 ✓  | כרטיסיות + משוב 3 דרגות                                          | **הושלם ואומת — אחרי תיקון.** הדירוגים נכתבים דרך Server Action ומאומתים ב-DB. הגרסה הראשונה נכשלה בשקט: ראה 11.3               |
| 10 ✓ | קוויז + תוצאות                                                   | **הושלם ואומת.** משוב מיידי, נכונה/שגויה, הסבר וניקוד                                                                           |
| 11 ✓ | מבחן תרגול לפי נושא + משותף                                      | **הושלם ואומת.** בחירת היקף (נושא בודד או כל החומר) ואורך; דגימה אקראית; ניקוד ב-DB דרך `submit_exam`                           |
| 12 ✓ | מאסטרי ותוצאות                                                   | **הושלם.** הקוויז נספר גם הוא — היה באג; נושאים חזקים וחלשים; `topic_mastery` מחשב משני המקורות                                 |
| 13 ✓ | ביקורת אבטחה                                                     | **הושלם.** נמצאה ונסגרה דליפה חוצת-משתמשים ב-`storage_path` — הפירוט ב-`supabase/README.md`. נשאר קליק אחד: הגנת סיסמאות דלופות |
| 14 ◐ | בדיקות                                                           | 21 בדיקות יחידה על הלוגיקה הטהורה, ב-`npm run verify`. חסר: בדיקות מקצה לקצה על הזרימה המלאה                                    |
| 15 ◐ | ייצור                                                            | האתר חי בוורסל מהענף הזה. חסר: `ANTHROPIC_API_KEY`, דומיין, ומדיניות פרטיות                                                     |

### 11.1 יציאה ל-Supabase — נפתר

הסביבה חסמה יציאה ל-`*.supabase.co`, ולכן שום קריאת HTTP מהאפליקציה
לשרת לא רצה. נפתח בהגדרות ה-egress של הסביבה (Custom + `*.supabase.co`,
עם רשימת ברירת המחדל מסומנת).

**מלכודת שנשארה, ושווה לזכור:** גם אחרי הפתיחה, `fetch` של Node המשיך
לקבל `Host not in allowlist` בזמן ש-`curl` לאותה כתובת עבר. הסיבה:
`curl` יוצא דרך ה-proxy ב-CONNECT, ו-Node לא משתמש ב-`HTTPS_PROXY`
כברירת מחדל — הוא יוצא ישירות ונתפס. הפתרון הוא `NODE_USE_ENV_PROXY=1`
בהרצה, ולא שינוי בקוד. מתועד ב-`AGENTS.md`.

### 11.2 אישור אימייל — החלטה פתוחה

בפרויקט **אישור האימייל דלוק** (ברירת המחדל של Supabase), והרשמה
מחזירה "בדוק את המייל" במקום להכניס מיד. הבעיה: שירות המייל המובנה
של Supabase מוגבל לכמה מיילים בשעה ואינו מיועד לייצור — כלומר
בהרשמה אמיתית רוב התלמידים פשוט לא יקבלו את הקישור.

שתי דרכים:

1. **לכבות אישור אימייל** (Authentication → Sign In / Providers →
   Confirm email). הרשמה מיידית, עומד ביעד "הרשמה בפחות מדקה". המחיר:
   אפשר להירשם עם כתובת שאינה שלך.
2. **לחבר SMTP חיצוני** (Resend / Postmark — יש חינם). נדרש ממילא
   בשביל שחזור סיסמה, ולכן זו הדרך הנכונה לפני השקה אמיתית.

הקוד תומך בשני המצבים כבר עכשיו ולא ישבר משינוי ההגדרה.

### 11.3 כישלון שקט שנתפס: דירוג הכרטיסיות

הגרסה הראשונה של הכרטיסיות שלחה את הדירוג מהדפדפן ישירות ל-Supabase
בשיגור-ושכח (`void`). המסך התקדם לכרטיסייה הבאה בלי לחכות לתשובה, ולכן
נראה שהכול עובד — אבל **אף שורה לא נכתבה**. הבדיקה עברה, והמאסטרי היה
מתבסס על טבלה ריקה.

מה שנחשף בבדיקה מול ה-DB (`select count(*) from flashcard_reviews` = 0),
ולא במסך.

**התיקון, ושתי מסקנות:**

1. כל הכתיבות של מסכי הלימוד עברו ל-Server Actions. הן רצות בשרת, ולכן
   אין בקשה חוצת־מקורות מהדפדפן ל-Supabase, וה-session מגיע מה-cookies
   כך ש-RLS חלה בדיוק כמו קודם.
2. שיגור-ושכח על כתיבה שמשנה מצב הוא באג, לא אופטימיזציה. הכפתור מחכה
   לתוצאה ומציג שגיאה אם נפלה.

**בדיקה שלא נוגעת ב-DB לא מוכיחה שנשמר משהו.** מסך שמתקדם הוא ראיה
לכך שהקוד בצד הלקוח רץ, לא לכך שהנתון קיים.

**אחרי כל שלב, לפני הבא:** הרצה, בדיקות, `tsc --noEmit`, lint, מבט אמיתי
בממשק ב-360px. לא בונים על קוד שבור.

---

## 12. מה נבדק

**המסע המלא:** נחיתה → הרשמה → יציאה → כניסה → דשבורד → העלאה → עיבוד →
סיכום → כרטיסיות → קוויז → תוצאות → מבחן → יציאה → כניסה מחדש → הנתונים שם.

**המקרים שנשברים:**

|                                  | ציפייה                                  |
| -------------------------------- | --------------------------------------- |
| `.exe` ששמו הוחלף ל-`.pdf`       | נדחה על magic bytes                     |
| קובץ 40MB                        | נדחה לפני העלאה, עם הודעה בעברית        |
| PDF ריק / דף מטושטש              | `failed` עם הסבר, לא קריסה              |
| המודל מחזיר JSON פגום            | `failed` עם הודעה ידידותית; השגיאה בלוג |
| המודל מסרב                       | הודעה בעברית, לא stack trace            |
| משתמש A ניגש לחומר של B          | 404/ריק — נחסם ב-RLS                    |
| מכסה חודשית נגמרה                | 429 עם הודעה מובנת                      |
| רשת נופלת באמצע העלאה            | ניתן לנסות שוב, בלי רשומת זומבי         |
| 360 / 390 / 430 / טאבלט / דסקטופ | כל מסך                                  |
| RTL                              | ראה סעיף 2.7                            |
| מצבים ריקים / טעינה / שגיאה      | לכל מסך יש שלושתם                       |

---

## 13. שתי ההחלטות, ולמה הן נסגרו כך

שתי השאלות הועלו, והתשובה שחזרה הייתה "כמו ש-Turbo AI עושים". הפנייה למתחרה
היא אינסטינקט נכון, אבל היא לא ניתנת למימוש כמו שהיא, ולכן ההחלטות נסגרו כאן.

### מה שידוע על Turbo AI, ומה לא

מדיניות הפרטיות שלהם נוקבת בשמות ספקי ה-AI: **OpenAI, Anthropic, xAI (Grok)
ו-Modal Labs**. כלומר אין "המודל של Turbo" — יש ניתוב בין כמה ספקים, ו-Modal
(פלטפורמת GPU) מרמזת שהם גם מריצים מודלים משלהם, קרוב לוודאי לתמלול. הניתוב
המדויק לכל משימה אינו פומבי, ו-`turbo.ai` חסום מהסביבה הזאת לבדיקה ישירה.

### למה זה לא מתאים להעתקה

1. **Turbo הוא קודם כול מתמלל הרצאות.** הקלט המרכזי שלו הוא אודיו, ובעיית
   החילוץ הקשה שלו היא דיבור←טקסט. הספק שלך פוסל אודיו במפורש ומתמקד ב-PDF
   ובצילומים. השיטה שלהם פותרת בעיה שאין לך, ולא נוגעת בזו שיש.
2. **Turbo הוא אנגלית-first.** שום דבר בגישה שלהם לא מטפל בהיפוך סדר הטקסט
   ב-PDF עברי או בכתב יד בעברית. הסיכון בסעיף 2.2 הוא שלך בלבד.
3. **שלושה ספקים ביום הראשון סותרים את סעיף 14 של הספק שלך.** שלושה מפתחות,
   שלושה חשבונות חיוב, שלושה מסלולי כשל, שלוש מערכות תנאים לגבי קטינים.
   הספק ביקש **הפשטת ספק** — אינטגרציה אחת מאחורי ממשק — לא שלוש חיות.

### ההחלטות

|            | מה נקבע        | הפיכות                                              |
| ---------- | -------------- | --------------------------------------------------- |
| קריאת חומר | **ראייה בלבד** | זו החלטת ארכיטקטורה; שינוי בהמשך = מסלול חילוץ נוסף |
| מודל       | **Opus 5**     | משתנה סביבה `LAMDAI_MODEL` — שורה אחת               |

### ובמקום לנחש — מודדים (נכנס לשלב 7)

אחרי שהעובד עובד מקצה לקצה, ולפני שהבטא נפתחת: אותם 10 חומרים אמיתיים —
PDF מוקלד, דף סרוק, ודף מחברת בכתב יד — עוברים דרך Opus 5, Sonnet 5 ו-Haiku 4.5.
לכל ריצה נמדדים העלות בפועל מתוך `model_calls`, וגם: האם הכרטיסיות באמת
מבוססות על החומר, האם המסיחים סבירים או אבסורדיים, והאם כתב היד פוענח נכון.
ההחלטה הסופית מתקבלת על הנתונים שלך, לא על ניחוש לגבי מתחרה.

מה שלא צריך החלטה ונקבע: פרנקפורט, מכסה של 8 העלאות למשתמש בחודש, מחיקת
קובץ המקור אחרי 24 שעות, `questions`/`attempts` כטבלאות מאוחדות, מאסטרי
כ-VIEW, ניקוד המבחן בשרת.

## 14. סבב העיצוב — מה השתנה ולמה

הממשק היה נכון ושקוף אבל שטוח, והמשוב היה חד: "לא אוהב את איך שהאתר
נראה, ובעיקר את הכרטיסיות". מה שנעשה:

- **הכרטיסייה הופכת באמת.** `rotateY(180deg)` על סצנה עם `perspective`,
  פנים לבן וגב שחור. במערכת מונוכרומית ההיפוך עצמו הוא האפקט, ואין
  צורך בצבע. `backface-visibility: hidden` על שתי הפאות, וכיבוי
  התנועה תחת `prefers-reduced-motion`.
- **הכרטיסייה נמצאת גם בדף הנחיתה.** המבקר הופך כרטיסייה אמיתית לפני
  שהוא נרשם, בחלק העליון של המסך. זו ההבטחה של המוצר בשלוש שניות.
- **טיפוגרפיה בוטה יותר.** `--text-hero` (4rem) לכותרת הנחיתה,
  `--text-figure` (4.5rem) לציון, `--text-display` (3.25rem) לכותרות
  מסך. בלי אותיות גדולות בעברית ההיררכיה נשענת רק על גודל ומשקל, ולכן
  הפער בין הרמות צריך להיות גדול.
- **משוב חד־משמעי בתרגול ובמבחן.** כל תשובה מקבלת אות (א/ב/ג/ד), ואחרי
  בחירה האות מתחלפת בווי או באיקס. קודם ההבדל היה רק גוון רקע.
- **לשוניות כבקרה מקטעית** במקום קו תחתון — שטח נגיעה גדול יותר בטלפון.
- **שתי תנועות בלבד במערכת:** `.rise` לכניסת תוכן, `.tap` ללחיצה. שתיהן
  מכובות תחת `prefers-reduced-motion`.
- **מסכי תוצאה שחורים** — ציון קוויז, ציון מבחן, סיכום סבב כרטיסיות.
  הרגע שבו התלמיד רואה כמה ידע הוא הרגע שמותר לו לצעוק.

הכול נבדק ב-360px וב-1280px, ו-`npm run verify` עובר.

## 15. סבב האיכות — הסיכום, הכרטיסיות והכמות

הפידבק היה על התוכן ולא רק על המראה: "הסיכום הוא הפיצ'ר אם לא הכי חשוב",
"חייב שיהיה מלא מלא שאלות", ו"אני רוצה שהתשובה תהיה ירוקה".

### הסיכום

- הפרומפט נכתב מחדש. הסיכום מוגדר בו כפריט החשוב ביותר, ונדרש להיות
  **מלא ומרוכז**: כיסוי של כל תהליך, מנגנון, הגדרה, מספר, שם וסיווג
  בחומר; בלי הקדמות ובלי משפטי מעבר; מונחים ומספרים נשמרים בדיוק כפי
  שהם, כי זו השפה שהמורה יבחן בה.
- המבנה השתנה: `summary_sections` — פרק לכל נושא, בסדר שבו הנושאים
  מופיעים בחומר. במסד נוספה עמודת `summaries.sections`, ועמודת `body`
  נשארה כאותו תוכן בטקסט אחד כדי שחומר שכבר עובד לא יישבר.
- `effort` עלה מ-`medium` ל-`high`, ו-`max_tokens` מ-32k ל-64k. חיסכון
  בשני הפרמטרים האלה נראה מיד כסיכום שמפספס חצי מהחומר.
- מסך הסיכום נבנה מחדש: עיקרי הדברים ככרטיס שחור בראש (מי שחוזר בערב
  המבחן מתחיל שם), פרקים עם כותרות, ומושגים ככרטיסים.

### הכרטיסיות

- **גב ירוק במקום שחור.** הירוק הוא צבע ה"נכון" במערכת, והתלמיד מזהה
  במבט אחד שהוא מסתכל על תשובה. הגוון (`--color-answer`) כהה מספיק
  לטקסט לבן ביחס ניגודיות ~6.5.
- הפרומפט דורש תשובה **שלמה**: שאלה עם שני חלקים מקבלת את שניהם, וכל
  פרט רלוונטי שהחומר נותן נכנס. "תשובה נכונה אך חלקית היא תשובה גרועה".

### הכמות

`quiz_questions` 8–25 (היה 4–12), `exam_questions` 15–60 (היה 8–40),
כרטיסיות 8–40 (היה 5–25), מושגים עד 30. הפרומפט מבקש במפורש למצות את מה
שהחומר מאפשר בלי לחזור על אותה שאלה בניסוח אחר.

**זה עולה כסף.** העלייה ב-effort ובכמות מכפילה בערך את עלות העיבוד
לחומר. תקרת ה-25$ לחודש נשארה כפי שהיא, ולכן היא תיתפס מוקדם יותר.
המנוף הזול ביותר הוא `supabase secrets set LAMDAI_MODEL=claude-sonnet-5`.

### הדמו כרף איכות

`src/lib/demoStudySet.ts` נכתב מחדש כדי לשקף בדיוק את מה שהפרומפט מבקש,
והדמו מציג את אותו `SummaryView` שמציג חומר אמיתי. אי אפשר שהדמו ייראה
טוב יותר מהמוצר. אגב כך תוקן באג בדמו: התשובה הנכונה הייתה במקום הראשון
בכל ארבע השאלות.

## 16. היקף — תרגול ומבחן על צירוף נושאים

עד כאן אפשר היה להיבחן על נושא בודד או על כל החומר, ולתרגל רק על הכול.
אבל מבחן אמיתי הוא בדיוק צירוף: "תורשה ומערכת העיכול", לא אחד מהם ולא
כל מה שהתלמיד העלה אי־פעם.

- `start_exam` מקבלת עכשיו `p_topic_ids uuid[]` במקום `p_topic_id uuid`.
  null או מערך ריק = כל החומר, כמו קודם. החתימה הישנה **נמחקה** ולא
  נשארה כעומס־יתר: שתי חתימות שנבדלות רק בסוג הפרמטר השני יוצרות קריאה
  דו־משמעית כשמעבירים null. תקרת השאלות עלתה מ-50 ל-100, כי מאגר המבחן
  גדל ל-60 וצירוף נושאים סוכם אותן.
- **התרגול קיבל בחירת היקף** שלא הייתה לו בכלל. הסינון נעשה בלקוח ולא
  במסד, כי שאלות התרגול ממילא מגיעות במלואן — בקוויז המשוב מיידי, וגם
  התשובה הנכונה כבר אצל הלקוח בכוונה (סעיף 5). `getQuestions` מחזירה
  עכשיו גם את שם הנושא.
- `ScopePicker` משותף לשניהם. "כל החומר" אינו פריט ברשימה אלא מתג שמסמן
  את כולם, ולכן אפשר גם להתחיל מהכול ולהוריד נושא אחד. אי אפשר לרוקן את
  הבחירה — מבחן בלי נושא אינו מבחן.
- כשיש נושא אחד בלבד אין מה לבחור, ומסך התרגול קופץ ישר לשאלות.

## 17. מובייל, והסרת המבחן מהניווט

### המבחן ירד מהניווט

נשארו שלוש לשוניות: סיכום, כרטיסיות, תרגול. אחרי שהתרגול קיבל בחירת
היקף (סעיף 16) הוא מכסה את אותו צורך, ולשונית רביעית רק פיצלה את המסך.

המסלול `/sets/[id]/exam` והקוד שלו — `ExamSetup`, `ExamRunner`, מסך
התוצאות ו-`start_exam` — נשארו במקומם ועובדים. ההחזרה היא שורה אחת
במערך `tabs` שב-`StudyTabs`. מה שכן השתנה: דף הנחיתה ודף המסלול
המורחב הפסיקו להבטיח פיצ'ר שלא נראה בממשק.

### מובייל

רוב התלמידים ייכנסו מהטלפון, ולכן המסכים נבדקו ב-360×640 עם `isMobile`
ו-`hasTouch`, לא רק בחלון צר. הבדיקה האוטומטית מודדת בכל מסך: גלישה
אופקית, אלמנטים רחבים מהמסך, שטחי נגיעה מתחת ל-40px וטקסט מתחת ל-12.5px.

מה שתוקן:

- **שטחי נגיעה של 44px.** כל קישורי הכותרת (שם המוצר, כניסה, הרשמה,
  יציאה), "חזרה לחומרים", "מחיקת החומר", "הקודמת" בכרטיסיות, "כרטיסייה
  הבאה" בדף הנחיתה וכפתור עריכת השם היו 17–22px. אצבע היא לא סמן עכבר.
  ה-padding מתקזז ב-margin שלילי כדי שהיישור הוויזואלי לא יזוז.
- **`-webkit-tap-highlight-color: transparent`** — הריבוע הכחול שהדפדפן
  מצייר על כל הקשה מסגיר "אתר" ולא מוצר. במקומו יש את אנימציית `.tap`.
- **`touch-action: manipulation`** — מבטל את השהיית 300ms שהדפדפן ממתין
  כדי לבדוק אם ההקשה היא הקשה כפולה לזום.
- **`text-size-adjust: 100%`** — ספארי בטלפון מנפח טקסט לבד במעבר לרוחב.
- **`user-select: none` על הכרטיסייה** — לחיצה ארוכה הופכת אותה במקום
  לסמן את הטקסט שבה.
- שדות הקלט כולם ב-16px לפחות. מתחת לזה iOS מזנק זום בפוקוס, והמשתמש
  נשאר עם דף מוגדל.
- הזום לא נחסם (`maximum-scale` לא מוגדר) — חסימת זום היא כשל נגישות.

אין אף מסך עם גלישה אופקית. החריג היחיד שנשאר הוא הקישור "כניסה" בתוך
משפט בתחתית ההרשמה — קישור טקסט בתוך פסקה, ומרווח אנכי שם היה שובר את
השורה.
